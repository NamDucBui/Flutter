include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"

linter:
  rules:
    prefer_const_constructors: true
    avoid_print: true
    # Disabled: project uses kebab-case file names per LLM-tooling convention
    file_names: false
